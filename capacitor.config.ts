import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cmsdle.neats',
  appName: 'N-Eats',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
